package it.unibo.risiko.model.deck;

import java.util.ArrayList;

/**
 * 
 * CardStrategy contains two methods:
 * cardsValid, checks whether a certain combination of cards can be played
 * troopsAvailable, returns the number of troops a player can use
 */
public class CardStrategy {
    void cardsValid(ArrayList<Card> setOfCards) {
        /* check whether the set of cards can be played */
    }

    int troopsAvailable() {
        /* needs to be a return method that returns the number of troops a player can use */
        return 0;
    }
}
