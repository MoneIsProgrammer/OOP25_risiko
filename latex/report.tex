\documentclass[a4paper,12pt]{report}

\usepackage{alltt, fancyvrb, url}
\usepackage{graphicx}
\usepackage[utf8]{inputenc}
\usepackage{float}
\usepackage{xcolor}
\usepackage{hyperref}

% Questo commentalo se vuoi scrivere in inglese.
\usepackage[italian]{babel}

\usepackage[italian]{cleveref}

\title{RisiKo! un mockup}

\author{Azhar Hamail, Morri Simone, Zangheri Marco}
\date{\today}


\begin{document}

\maketitle

\begin{abstract}
Questo progetto porta a riprodurre il gioco del RisiKo!, o come viene conosciuto al di fuori dell'italia Risk, in modo da simularne, sebbene in maniera semplificata, comportamenti in modo da avere un'esperienza di gioco fluida ed equiparabile a quella del gioco da tavolo con la capacità di poter giocare in assenza di altri giocatori contro il computer.
\end{abstract}

\tableofcontents

\chapter{Analisi}
\section{Descrizione e requisiti}
Il software vuole essere un mockup di risiko utilizzando come riferimento il manuale fornito da risiko.it\footnote{https://risiko.it/wp-content/uploads/2017/10/Regolamento-Risiko.pdf} prendendo una regola da torneo, ovvero che il difensore si difende sempre col masimo dell sue forze, e ignorando per semplicità tutte le atrre regole da torneo.
\newline RisiKo ha vari elementi distinti fra di loro necessari per il funzionamento del gioco tra cui:
\begin{itemize}
\item Un tabellone di gioco dove sono rappresentati i vari territori.
\item Truppe di vario colore per distinguere i giocatori e segnare il numero di armate in uno specifico territorio.
\item Un mazzo di carte raffiguranti una figura e un territorio da utilizzare sia in fase iniziale per definire i territori posseduti ad inizio partita, sia come bonus per ottenere truppe bonus ad inizio del proprio turno.
\item Delle carte che rappresentano vari obbiettivi per determinare il vincitore della partita.
\item Dadi per risolvere le azioni di gioco.
\item Dalle 3 alle 8 persone per poter giocare se si seguono le regole classiche.
\end{itemize}
Questo ci porta ad avere dei giocatori che devono essere consapevoli delle loro potenziali azioni, una mappa che tiene traccia della disposizione di territori e truppe e un mazzo che fornisce carte ai giocatori.
\newline Nello specifico una partita comincia prendendo almeno 3 giocatori, gli si consegnano delle carte che determinano i territori iniziali che poi vengono rimescolate, dopo aver piazzato a turno tutte le truppe disponibili sul tabellone e aver preso la propria carta obbiettivo comincia la partita con il primo giocatore scelto con un lancio di dado, a quel punto un turno si divide in rinforzo, attacco e movimento, si continua così finche un giocatore non raggiunge il suo obbiettivo a fine del suo turno.
\subsection{Requisiti funzionali}
\begin{itemize}
    \item Un gestore della partita per definire quali azioni sono possibili e differenziare le varie fasi di attacco, movimento e rinforzo.
    \item Un mazzo che può simulare le azioni di un mazzo reale(Mischiare le carte, dare le carte, etc...).
    \item Una mappa che tiene traccia delle armate presenti e mostra quali territori formano vari continenti.
    \item Bot che possono giocare in mancanza di giocatori umani.
\end{itemize}


\section{Modello del Dominio
}
Durante una partita i giocatori possono consultare il tabellone che rappresenta la mappa di gioco contenente i vari continenti che a loro volta sono composti da  territori che contengono le armate posizionate da ciascun giocatore. Un territorio si definisce in possesso di uno specifico giocatore se quest'ultimo ha delle armate nel territorio.
\newline Ogni giocatore dispone di una propria mano, ovvero delle carte fornite dal mazzo composto da queste ultime.
\newline I giocatori che possono essere umani o aventi un ai, entrambi generano degli eventi a seguito delle loro azioni, interagendo con la mappa.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{latex/resources/ModelloDominio.png}
    
    \label{fig:placeholder}
\end{figure}

\chapter{Design}

In questo capitolo si spiegano le strategie messe in campo per soddisfare i requisiti identificati nell'analisi.

Si parte da una visione architetturale, il cui scopo è informare il lettore di quale sia il funzionamento dell'applicativo realizzato ad alto livello.
%
In particolare, è necessario descrivere accuratamente in che modo i componenti principali del sistema si coordinano fra loro.
%
A seguire, si dettagliano alcune parti del design, quelle maggiormente rilevanti al fine di chiarificare la logica con cui sono stati affrontati i principali aspetti dell'applicazione.

\section{Architettura}

Abbiamo usato il pattern MVC, con il codice diviso nei package \texttt{model}, \texttt{view} e \texttt{controller}.

La parte che ho fatto io è divisa così: mappa, dadi e combattimento stanno nel model e non sanno niente né della grafica né del controller; il disegno della mappa sta nella view, ma da fuori si usa solo tramite l'interfaccia \texttt{MapView}, che non contiene tipi di JavaFX; \texttt{MapClickHandler} sta nel controller, riceve i click dalla view, legge la mappa e non modifica niente. Per tenere aggiornato lo schermo uso il pattern Observer: \texttt{MapView} estende \texttt{GameObserver}, quindi riceve gli \texttt{Event} e la mappa si ridisegna da sola.

Per la mia parte cambiare libreria grafica vuol dire riscrivere solo \texttt{MapCanvas} e \texttt{DiceCanvas}: il model e \texttt{MapClickHandler} restano uguali.

% TODO Simone: aggiungi qui la tua parte, cioè i punti d'ingresso di giocatori, eventi e storico,
% e come si parlano GameController e GameScene. Segnala anche che GameController conosce
% direttamente GameScene e usa property di JavaFX, e che History usa le liste di JavaFX.
% TODO Hamail: aggiungi la tua parte, carte e fasi del turno, e come si aggancia al resto.
% TODO: lo schema UML dell'architettura e' gia' pronto, lo aggiungo quando ci saranno
% anche il mazzo e le fasi del turno, se no e' incompleto.


Questa sezione spiega come le componenti principali del software interagiscono fra loro.
%
In particolare, qui va spiegato \textbf{se} e \textbf{come} è stato utilizzato il pattern
architetturale model-view-controller (e/o alcune sue declinazioni specifiche, come entity-control-boundary).

Se non è stato utilizzato MVC, va spiegata in maniera molto accurata l'architettura scelta, giustificandola in modo appropriato.

Se è stato scelto MVC, vanno identificate con precisione le interfacce e classi che rappresentano i punti d'ingresso per modello, view, e controller.
Raccomandiamo di sfruttare la definizione del dominio fatta in fase di analisi per capire quale sia l'entry point del model, e di non realizzare un'unica macro-interfaccia che, spesso, finisce con l'essere il prodromo ad una ``God class''.
%
Consigliamo anche di separare bene controller e model, facendo attenzione a non includere nel secondo strategie d'uso che appartengono al primo.

In questa sezione vanno descritte, per ciascun componente architetturale che ruoli ricopre (due o tre ruoli al massimo), ed in che modo interagisce (ossia, scambia informazioni) con gli altri componenti dell'architettura.
%
Raccomandiamo di porre particolare attenzione al design dell'interazione fra view e controller: se ben progettato, sostituire in blocco la view non dovrebbe causare alcuna modifica nel controller (tantomeno nel model).

\subsection*{Elementi positivi}
\begin{itemize}
 \item Si mostrano pochi, mirati schemi UML dai quali si deduce con chiarezza quali sono le parti principali del software e come interagiscono fra loro.
 \item Si mette in evidenza se e come il pattern architetturale model-view-controller è stato applicato, anche con l'uso di un UML che mostri le interfacce principali ed i rapporti fra loro.
 \item Si discute se sia semplice o meno, con l'architettura scelta, sostituire in blocco la view:
 	in un MVC ben fatto, controller e modello non dovrebbero in alcun modo cambiare se si transitasse da una libreria grafica ad un'altra (ad esempio, da Swing a JavaFX, o viceversa).
\end{itemize}

\subsection*{Elementi negativi}
\begin{itemize}
 \item L'architettura è fatta in modo che sia impossibile riusare il modello per un software diverso che affronta lo stesso problema.
 \item L'architettura è tale che l'aggiunta di una funzionalità sul controller impatta pesantemente su view e/o modello.
 \item L'architettura è tale che la sostituzione in blocco della view impatta sul controller o, peggio ancora, sul modello.
 \item Si presentano UML caotici, difficili da leggere.
 \item Si presentano UML in cui sono mostrati elementi di dettaglio non appartenenti all'architettura, ad esempio includenti campi o con metodi che non interessano la parte di interazione fra le componenti principali del software.
 \item Si presentano schemi UML con classi (nel senso UML del termine) che ``galleggiano'' nello schema, non connesse, ossia senza relazioni con il resto degli elementi inseriti.
 \item Si presentano elementi di design di dettaglio, ad esempio tutte le classi e interfacce del modello o della view.
 \item Si discutono aspetti implementativi, ad esempio eventuali librerie usate oppure dettagli di codice.
\end{itemize}

\section{Design dettagliato}

\subsection {Far produrre allo stesso tipo di entità risultati differenti. Morri Simone}
Il primo e principale problema è : come far prendere decisioni o/e avere comportamenti diversi in base al tipo di giocatore, qui mi sono imbattuto in una necessità di dare a ogni giocatore in campo il proprio comportamento, quindi lo Strategy Pattern è stato essenziale nella progettazione. Questo pattern mi permette di ridurre le implementazioni di player da quanti comportamenti diversi volevo a 1 dove espone ciò che un vero giocatore può fare, delegando le decisioni a PlayerStrategy dove ogni sua implementazione può fornire un risultato diverso, nel caso delle ai producono un risultato in base al contesto fornito e i giocatori umani tramite dei Builder interni possono costruire il risultato passo per passo. Facendo così non solo ho ridotto la quantità di codice duplicato ma è anche possibile aggiungerne diversi tipi di strategie ed eventualmente anche combinarne varie in uno stesso giocatore.
%
\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\linewidth]{latex//resources/PlayerStrategyDiagram.png}
    \caption{Strategy per i Player}
    \label{fig:Strategy}
\end{figure}
\subsection{Comunicazione per i Player. Morri Simone}
Un altro problema è come comunicare le intenzioni dei giocatori al controller, quindi ho creato dei DTO nominandoli Event in modo da poter impacchettare tutto ciò che potrebbe servire ad un controller per eseguire l'azione, utili anche per poter creare uno storico della partita, fin da subito però sono sorti problemi in quanto esistono diversi tipi di eventi tutti con campi diversi, ciò rende impossibile utilizzarli senza fare un cast esplicito, ma anche qui non sappiamo la loro classe originale, quindi utilizzando il Visitor Pattern è possibile dare un'implementazione specifica a seconda di cosa voglio generare a partire da uno specifico Event, questa cosa è tornata utile nella History che è passata da un contenitore di eventi a un contenitore di Stringhe che rappresentano gli eventi attraverso una implementazione specifica di Visitor. Utilizzando questo pattern inoltre permette anche ad altro programmatori di modificare gli eventi per ad esempio renderli persistenti oppure esportarli in altri formati per il salvataggio in ad esempio Json.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{latex/resources/EventVisitor.png}
    \caption{Modello per visitor}
    \label{fig:Visitor}
\end{figure}
\subsection{Creare i Giocatori}
Un ultimo problema non meno importante è come far costruire i vari giocatori che parteciperanno alla partita, visto che ognuno deve avere delle specifiche proprie. Qui utilizzando sempre dei DTO chiamati PlayerRequest posso gestire la creazione astraendo il giocatore dalla richiesta di creazione, e per rendere la creazione più pulita e facile da fare ho creato una Simple Factory che riceve le varie richieste dei giocatori e produce una lista di Player pronti da utilizzare create sotto le specifiche e per aderire all'utilizzo di ID proposto il tutto viene raccolto in Roster che alla creazione richiede tutte le richieste e permette di interrogare i giocatori chiedendo chi è il possessore di uno specifico ID o Colore,quest'ultimo astratto in RisikoColors. 
\begin{figure}[h]
    \centering
    \includegraphics[width=0.5\linewidth]{latex/resources/FactoryPipeline.png}
    \caption{The player creation pipeline}
    \label{fig:factoryPipeline}
\end{figure}

\subsection{Zangheri Marco. Costruire la mappa e collegarla ai giocatori}
La mappa ha 42 territori, 6 continenti e 83 confini. Scriverla nel codice voleva dire centinaia di righe e ricompilare ogni volta che sbagliavo un confine, quindi l'ho messa in un file di testo, \texttt{world.txt}, che \texttt{MapLoader} legge riga per riga. \texttt{MapLoader} però non controlla niente: passa tutto a \texttt{GameMapBuilder}, e qui ho usato il pattern Builder. Il builder raccoglie continenti, territori e confini e solo alla fine, in \texttt{build()}, controlla che la mappa abbia senso: niente id doppi, niente confini verso territori che non esistono, nessun territorio isolato. Così una mappa sbagliata non si può creare: o esce giusta o ti dà errore.

Poi c'era da decidere come legare i territori ai giocatori. Avevamo valutato anche l'alternativa di far restituire a \texttt{Territory} direttamente il \texttt{Player} proprietario, che era più comoda, ma così la mappa dipendeva dai giocatori, che a loro volta usano già la mappa, e non si poteva più usare né testare da sola. Ho tenuto quindi solo l'id del proprietario, e anche i confini sono id. Per decidere ci siamo basati sulla meta-relazione, che tra gli errori di architettura mette proprio un modello che non si può riusare. Il prezzo è che l'id è una \texttt{String} e il compilatore non ti ferma se ne passi uno sbagliato. Per non riscrivere ogni volta la ricerca per id ho fatto \texttt{Registry<T extends Identifiable>}, che poi è stato usato anche per i giocatori.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{latex/resources/MapBuilder.png}
    \caption{Costruzione della mappa}
    \label{fig:MapBuilder}
\end{figure}

\subsection{Zangheri Marco. Dadi sostituibili e combattimento}
Per il combattimento il problema era testarlo: con i dadi veri il risultato cambia ogni volta e non puoi controllare se le perdite sono giuste. Quindi \texttt{Dice} l'ho fatto come interfaccia (pattern Strategy): in partita \texttt{CombatSystemImpl} usa \texttt{RandomDice}, nei test gli passo \texttt{FixedDice}, un dado finto a cui dico prima cosa deve uscire.

\texttt{resolve} tira i dadi, li confronta a coppie partendo dal più alto (in parità vince chi difende) e restituisce un \texttt{BattleResult} con i dadi usciti, le perdite di tutti e due e se il territorio è conquistato. All'inizio mi sembrava una complicazione che \texttt{resolve} non tocchi la mappa, e che le armate le debba togliere chi gestisce il turno, ma è proprio quello che lo rende facile da testare: c'è un test apposta che controlla che dopo uno scontro i territori siano rimasti uguali.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{latex/resources/Combat.png}
    \caption{Dadi sostituibili e combattimento}
    \label{fig:Combat}
\end{figure}

\subsection{Zangheri Marco. La mappa a schermo}
La mappa si deve ridisegnare dopo ogni attacco o spostamento, ma chiamarla dal codice del turno voleva dire mettere la grafica dentro alla logica di gioco. Ho usato il pattern Observer: \texttt{GameObserver} ha un solo metodo, \texttt{onEvent}, la mappa lo implementa e si aggiorna da sola quando le arriva un evento. Per capire se un evento cambia il disegno uso un visitor, \texttt{RedrawIfNeeded} dentro \texttt{MapCanvas}: un attacco appena dichiarato no, il risultato di un attacco sì. Qui ho visto pro e contro del visitor: quando Simone ha aggiunto \texttt{CardEvent} la mia classe non compilava più finché non ho deciso cosa farne. Fastidioso, però me ne sono accorto subito e non giocando.

Il controller non vede mai \texttt{MapCanvas}, lo crea solo la schermata di gioco, e usa l'interfaccia \texttt{MapView}, dentro cui non c'è niente di JavaFX: i colori sono i \texttt{RisikoColors} del model e i territori sono id. Le posizioni stanno su una griglia fissa di 1000 per 600 e \texttt{ViewTransform} le riscala sulla finestra; siccome disegno e click usano la stessa trasformazione, il territorio che clicchi è sempre quello che vedi.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{latex/resources/MapView.png}
    \caption{La mappa come osservatore degli eventi}
    \label{fig:MapView}
\end{figure}

\subsection{Zangheri Marco. Cosa vuol dire un click}
La mappa sa solo quale territorio hai cliccato, ma lo stesso click vuol dire cose diverse in base alla fase: in attacco il primo click è da dove attacchi e il secondo il bersaglio, nello spostamento il secondo è dove vai. Invece di mettere queste regole in \texttt{MapCanvas} ho fatto \texttt{MapClickHandler}, nel controller: col primo click scegli un tuo territorio con almeno due armate e la mappa evidenzia dove puoi andare, col secondo, se la destinazione va bene, avvisa chi ascolta e gli passa i due id. La mossa non la crea lui, quella resta alla parte dei giocatori.

All'inizio c'era un metodo solo, \texttt{setTurn(giocatore, fase)}, perché pensavo che chi faceva il gestore dei turni avesse sempre tutte e due le cose insieme. Con \texttt{PlayerTurn} di Simone si è visto che non è così: la fase cambia più volte nello stesso turno e viene annunciata da sola, il giocatore cambia una volta per turno. L'ho quindi diviso in \texttt{setPlayer} e \texttt{setPhase}, e ho collegato \texttt{setPhase} ai cambi di fase. Poi ho collegato anche \texttt{setPlayer}: \texttt{PlayerTurn} annuncia chi gioca a ogni cambio di turno e alla fine della preparazione, con un avviso separato da quello delle fasi.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{latex/resources/Click.png}
    \caption{Gestione dei click}
    \label{fig:Click}
\end{figure}

\subsection{Hamail Azhar. Costruire due tipi di mazzi}
In un mazzo di carte territorio ci sono 42 carte di tipo territorio e altre 2 carte jolly mentre nel mazzo di carte obiettivo ci sono 14 carte obiettivo. Facendo prima questa distinzione, ho pensato di creare due classi \texttt{ObjectivesDeck} e \texttt{TerritoriesDeck}, per costruire i due mazzi, i metodi comuni a questi mazzi sono stati inseriti in una terza classe \texttt{Deck}. Nella classe deck ci sono metodi per popolare un mazzo di carte \texttt{populateTerritoryDeck} e \texttt{populateObjectiveDeck} a partire da classi enum (\texttt{Objective}, \texttt{CardTerritories} per aggiungere i territori alle carte territorio e \texttt{CardTroops} per aggiungere le truppe alle carte territorio). Inoltre, per aggiungere le carte jolly, che non hanno nessun territorio, ho usato un metodo \texttt{addJolly} che crea solo due carte jolly. Nella classe \texttt{TerritoriesDeck}, ci sono due metodi, uno per creare il mazzo con le carte territorio e uno per aggiungere le carte jolly. Entrambi però contengono un metodo shuffle che mischia le carte dopo che sono state inserite nel mazzo. Questi metodi vengono poi usati nella fasi di preparazione. Per risolvere il problema di ripopolare il mazzo dopo che si è svuotato, ho aggiunto un controllo in \texttt{dealCard} che prima di distribuire una carta, controlla se il mazzo è vuoto e se lo è, lo ripopola da solo.

\subsection{Hamail Azhar. Condizioni di vittoria}
Per le carte, ho creato una classe comune \texttt{Card} che contiene costruttori per le varie carte, che possono essere carte territorio, carte jolly e carte obiettivo. Per controllare se un giocatore aveva raggiunto il suo obiettivo (assegnatogli durante la \texttt{SetupPhase}) ho prima creato due metodi \texttt{checkElimination} e \texttt{managePlayerElimination} dentro la \texttt{MovePhase}. Dopo che il giocatore ha spostato le sue truppe, è arrivato alla fine del suo turno, quindi con \texttt{checkElimination} controllo se qualche giocatore è stato eliminato e se è stato eliminato chiamo \texttt{managePlayerElimination} che controlla se eliminarlo era l'obiettivo di qualcuno. Questo metodo ritorna true solo se l'obiettivo di eliminare quel giocatore era del giocatore corrente, il che significa che il giocatore corrente ha vinto. Altrimenti, se l'obiettivo di eliminarlo era di un'altro giocatore, gli assegno un nuovo obiettivo (conquista 24 territori).
Se poi \texttt{managePlayerElimination} non ha restituito true, chiamo \texttt{victoryCheck} per sapere se il giocatore corrente ha raggiunto il suo obiettivo. Dentro \texttt{victoryCheck}, prima controllo (tramite switch) qual'era l'obiettivo del giocatore corrente e poi nel case controllo se ha raggiunto quel obiettivo.

\subsection{Hamail Azhar. Armate bonus in base al combo di carte giocato}
La classe \texttt{CardBonus} serve per controllare se un giocatore ottiene delle armate bonus in base a un tris di carte che gioca. Il metodo principale è \texttt{bonusReinforcements} che prima calcola il numero di armate bonus in base al numero di territori occupati dal giocatore e poi chiama \texttt{calculateThreeBonus} per vedere se il combo di carte che il giocatore vuole giocare, gli assegnano delle armate in più. Per cui, prima calcola il numero di carte jolly, poi assegna truppe in base alla combinazione di carte giocata.

In questa sezione si possono approfondire alcuni elementi del design con maggior dettaglio.
%
Mentre ci attendiamo principalmente (o solo) interfacce negli schemi UML delle sezioni precedenti,
in questa sezione è necessario scendere in maggior dettaglio presentando la struttura di alcune sottoparti rilevanti dell'applicazione.
%
È molto importante che, descrivendo la soluzione ad un problema, quando possibile si mostri che non si è re-inventata la ruota ma si è applicato un design pattern noto.
%
Che si sia utilizzato (o riconosciuto) o meno un pattern noto, è comunque bene definire qual è il problema che si è affrontato, qual è la soluzione messa in campo, e quali motivazioni l'hanno spinta.
%
È assolutamente inutile, ed è anzi controproducente, descrivere classe-per-classe (o peggio ancora metodo-per-metodo) com'è fatto il vostro software: è un livello di dettaglio proprio della documentazione dell'API (deducibile dalla Javadoc).

\textbf{È necessario che ciascun membro del gruppo abbia una propria sezione di design dettagliato,
di cui sarà il solo responsabile}.
%
Ciascun autore dovrà spiegare in modo corretto e giustamente approfondito (non troppo in dettaglio, non superficialmente) il proprio contributo.
%
È importante focalizzarsi sulle scelte che hanno un impatto positivo sul riuso, sull'estensibilità, e sulla chiarezza dell'applicazione.
%
Esattamente come nessun ingegnere meccanico presenta un solo foglio con l'intero progetto di una vettura di Formula 1, ma molteplici fogli di progetto che mostrano a livelli di dettaglio differenti le varie parti della vettura e le modalità di connessione fra le parti, così ci aspettiamo che voi, futuri ingegneri informatici, ci presentiate prima una visione globale del progetto, e via via siate in grado di dettagliare le singole parti, scartando i componenti che non interessano quella in esame.
%
Per continuare il parallelo con la vettura di Formula 1, se nei fogli di progetto che mostrano il
design delle sospensioni anteriori appaiono pezzi che appartengono al volante o al turbo, c'è una
chiara indicazione di qualche problema di design.

Si divida la sezione in sottosezioni, e per ogni aspetto di design che si vuole approfondire, si presenti:
\begin{enumerate}
    \item: una breve descrizione in linguaggio naturale del problema che si vuole risolvere, se necessario ci si può aiutare con schemi o immagini;
    \item: una descrizione della soluzione proposta, analizzando eventuali alternative che sono state prese in considerazione, e che descriva pro e contro della scelta fatta;
    \item: uno schema UML che aiuti a comprendere la soluzione sopra descritta;
    \item: se la soluzione è stata realizzata utilizzando uno o più pattern noti, si spieghi come questi sono reificati nel progetto
    (ad esempio: nel caso di Template Method, qual è il metodo template;
    nel caso di Strategy, quale interfaccia del progetto rappresenta la strategia, e quali sono le sue implementazioni;
    nel caso di Decorator, qual è la classe astratta che fa da Decorator e quali sono le sue implementazioni concrete; eccetera);
\end{enumerate}
%
La presenza di pattern di progettazione \emph{correttamente utilizzati} è valutata molto positivamente.
%
L'uso inappropriato è invece valutato negativamente: a tal proposito, si raccomanda di porre particolare attenzione all'abuso di Singleton, che, se usato in modo inappropriato, è di fatto un anti-pattern.

\subsection*{Elementi positivi}

\begin{itemize}
	\item Ogni membro del gruppo discute le proprie decisioni di progettazione, ed in particolare le azioni volte ad anticipare possibili cambiamenti futuri (ad esempio l'aggiunta di una nuova funzionalità, o il miglioramento di una esistente).
	\item Si mostrano le principali interazioni fra le varie componenti che collaborano alla soluzione di un determinato problema.
	\item Si identificano, utilizzano \textit{appropriatamente}, e descrivono diversi design pattern.
	\item Ogni membro del gruppo identifica i pattern utilizzati nella sua sottoparte.
	\item Si mostrano gli aspetti di design più rilevanti dell'applicazione, mettendo in luce la maniera in cui si è costruita la soluzione ai problemi descritti nell'analisi.
	\item Si tralasciano aspetti strettamente implementativi e quelli non rilevanti, non mostrandoli negli schemi UML (ad esempio, campi privati) e non descrivendoli.
	\item Ciascun elemento di design identificato presenta una piccola descrizione del problema calato
nell'applicazione, uno schema UML che ne mostra la concretizzazione nelle classi del progetto, ed
una breve descrizione della motivazione per cui tale soluzione è stata scelta, specialmente se è stato utilizzato un pattern noto. Ad esempio, se si
dichiara di aver usato Observer, è necessario specificare chi sia l'observable e chi l'observer; se
si usa Template Method, è necessario indicare quale sia il metodo template; se si usa Strategy, è
necessario identificare l'interfaccia che rappresenta la strategia; e via dicendo.
\end{itemize}

\subsection*{Elementi negativi}
\begin{itemize}
	\item Il design del modello risulta scorrelato dal problema descritto in analisi.
	\item Si tratta in modo prolisso, classe per classe, il software realizzato, o comunque si riduce la sezione ad un mero elenco di quanto fatto.
	\item Non si presentano schemi UML esemplificativi.
	\item Non si individuano design pattern, o si individuano in modo errato (si spaccia per design pattern qualcosa che non lo è).
	\item Si utilizzano design pattern in modo inopportuno. Un esempio classico è l'abuso di
Singleton per entità che possono essere univoche ma non devono necessariamente esserlo. Si rammenta
che Singleton ha senso nel secondo caso (ad esempio \texttt{System} e \texttt{Runtime} sono
singleton), mentre rischia di essere un problema nel secondo. Ad esempio, se si rendesse singleton
il motore di un videogioco, sarebbe impossibile riusarlo per costruire un server per partite online
(dove, presumibilmente, si gestiscono parallelamente più partite).
	\item Si producono schemi UML caotici e difficili da leggere, che comprendono inutili elementi di dettaglio.
	\item Si presentano schemi UML con classi (nel senso UML del termine) che ``galleggiano'' nello schema, non connesse, ossia senza relazioni con il resto degli elementi inseriti.
	\item Si tratta in modo inutilmente prolisso la divisione in package, elencando ad esempio le classi una per una.
\end{itemize}

\subsection*{Esempio minimale (e quindi parziale) di sezione di progetto con UML ben realizzati}

\subsubsection{Personalità intercambiabili}

\paragraph{Problema} GLaDOS ha più personalità intercambiabili, la cui presenza deve essere trasparente al client.

\paragraph{Soluzione} Il sistema per la gestione della personalità utilizza il \textit{pattern Strategy}, come da
 le implementazioni di \texttt{Personality} possono essere modificate, e la
modifica impatta direttamente sul comportamento di GLaDOS.

\subsubsection{Riuso del codice delle personalità}
\paragraph{Problema} In fase di sviluppo, sono state sviluppate due personalità, una buona ed una cattiva.
Quella buona restituisce sempre una torta vera, mentre quella cattiva restituisce sempre la
promessa di una torta che verrà in realtà disattesa.
Ci si è accorti che diverse personalità condividevano molto del comportamento,
portando a classi molto simili e a duplicazione.

\paragraph{Soluzione} Dato che le due personalità differiscono solo per il comportamento da effettuarsi in caso di percorso completato con successo,
è stato utilizzato il \textit{pattern template method} per massimizzare il riuso, come da
Il metodo template è \texttt{onSuccess()}, che chiama un metodo astratto e protetto
\texttt{makeCake()}.

\subsubsection{Gestione di output multipli}

\paragraph{Problema} Il sistema deve supportare output multipli. In particolare, si richiede che vi sia un logger che stampa a terminale o su file,
e un'interfaccia grafica che mostri una rappresentazione grafica del sistema.

\paragraph{Soluzione} Dato che i due sistemi di reporting utilizzano le medesime informazioni, si è deciso di raggrupparli dietro l'interfaccia \texttt{Output}.
A questo punto, le due possibilità erano quelle di far sì che \texttt{GLaDOS} potesse pilotarle entrambe.
Invece di fare un sistema in cui questi output sono obbligatori e connessi, si è deciso di usare maggior flessibilità (anche in vista di future estensioni)
e di adottare una comunicazione uno-a-molti fra \texttt{GLaDOS} ed i sistemi di output.
La scelta è quindi ricaduta sul \textit{pattern Observer}: \texttt{GLaDOS} è observable, e le istanze di \texttt{Output} sono observer.
%
Il suo utilizzo è esemplificato in 


\subsection*{Contro-esempio: pessimo diagramma UML}

In  è mostrato il modo \textbf{sbagliato} di fare le cose.
%
Questo schema è fatto male perché:
\begin{itemize}
	\item È caotico.
	\item È difficile da leggere e capire.
	\item Vi sono troppe classi, e non si capisce bene quali siano i rapporti che intercorrono fra loro.
	\item Si mostrano elementi implementativi irrilevanti, come i campi e i metodi privati nella classe \texttt{AbstractEnvironment}.
	\item Se l'intenzione era quella di costruire un diagramma architetturale, allora lo schema è ancora più sbagliato, perché mostra pezzi di implementazione.
	\item Una delle classi, in alto al centro, galleggia nello schema, non connessa a nessuna altra classe, e di fatto costituisce da sola un secondo schema UML scorrelato al resto
	\item Le interfacce presentano tutti i metodi e non una selezione che aiuti il lettore a capire quale parte del sistema si vuol mostrare.
\end{itemize}


\chapter{Sviluppo}
\section{Testing automatizzato}

La parte di testing  per i giocatori ho dovuto sottoporre a testing praticamente ogni mia implementazione delle strategy verificando che dessero l'output desiderato su una mappa fittizia e i vari builder di HumanStrategy producessero un output corretto, l'unico test che non è possibile considerare automatizzato è quello riguardante la RandomStrategy in quanto la sua implementazione sarebbe dovuta essere cambiata troppo, infatti anche fornendogli un seed gli output cambiano se vengono eseguiti tutti i test o no.

\subsection{Zangheri Marco}
Ho testato con JUnit 5 la parte di model: mappa, lettura del file, posizioni dei territori e combattimento, 14 test in tutto. Per mappa e combattimento uso mappe piccole fatte a mano, così ogni test prova una cosa sola e non dipende dal file, e i dadi li do io con \texttt{FixedDice}. \texttt{MapLoaderTest} e \texttt{MapLayoutTest} invece caricano la mappa vera e controllano che il file sia giusto: 42 territori, 6 continenti, confini che valgono nei due sensi e coordinate per ogni territorio, perché un territorio senza coordinate sarebbe in gioco ma non lo vedresti.

Per \texttt{MapCanvas}, \texttt{DiceCanvas} e \texttt{MapClickHandler} non ho fatto test automatici per mancanza di tempo. \texttt{MapClickHandler} si potrebbe testare con una \texttt{MapView} finta, visto che non usa JavaFX.



\subsection*{Elementi positivi}

\begin{itemize}
 \item Si descrivono molto brevemente i componenti che si è deciso di sottoporre a test automatizzato.
 \item Si utilizzano suite specifiche (e.g. JUnit) per il testing automatico.
\end{itemize}

\subsection*{Elementi negativi}
\begin{itemize}
 \item Non si realizza alcun test automatico.
 \item La non presenza di testing viene aggravata dall'adduzione di motivazioni non valide. Ad esempio, si scrive che l'interfaccia grafica non è testata automaticamente perché è \emph{impossibile} farlo\footnote{Testare in modo automatico le interfacce grafiche è possibile (si veda, come esempio, \url{https://github.com/TestFX/TestFX}), semplicemente nel corso non c'è modo e tempo di introdurvi questo livello di complessità. Il fatto che non vi sia stato insegnato come farlo non implica che sia impossibile!}.
 \item Si descrive un testing di tipo manuale in maniera prolissa.
 \item Si descrivono test effettuati manualmente che sarebbero potuti essere automatizzati, ad esempio scrivendo che si è usata l'applicazione manualmente.
 \item Si descrivono test non presenti nei sorgenti del progetto.
 \item I test, quando eseguiti, falliscono.
\end{itemize}

\section{Note di sviluppo}

\subsection{Morri Simone}
\begin{itemize}
    \item Stream, Lambda e Optional utilizzo pervasivo \href{https://github.com/MoneIsProgrammer/risiko/blob/1ef3e0756d3ff5c10e6d098ca0f10d445c8c83aa/src/main/java/it/unibo/risiko/model/player/strategy/ai/AggressiveStrategy.java#L67-L94}{ad esempio}
    \item JavaFx e le sue componenti \href{https://github.com/MoneIsProgrammer/risiko/blob/1ef3e0756d3ff5c10e6d098ca0f10d445c8c83aa/src/main/java/it/unibo/risiko/view/PlayerSelectScene.java#L176-L223}{ad esempio}

\end{itemize}
Inoltre potrebbero essere presenti pattern di codice simile a quello altrui in quanto per imparare (principalmente JavaFx) ho seguito guide e discussioni sui forum su come utilizzare vari componenti che mi servivano. L'unico pezzo di codice copiato è \href{https://github.com/MoneIsProgrammer/risiko/blob/1ef3e0756d3ff5c10e6d098ca0f10d445c8c83aa/src/main/java/it/unibo/risiko/model/player/strategy/StrategyUtils.java#L106-L108}{questo} dove appare più volte per risolvere lo stesso problema di ottenere combinazioni di un set di oggetti, appare come metodo di libreria di ApacheCommons, ma non volendo importare una intera libreria solo per uno snippet di codice lungo 3 righe la fonte più "affidabile" che ho trovato è probabilmente una \href{https://www.cs.cornell.edu/courses/cs1112/2011sp/Notes/egL20/L20post.pdf?}{slide} di una lezione della Cornell, nello specifico la numero 16.

\subsection{Zangheri Marco}
\begin{itemize}
    \item Generici con parametro vincolato, \texttt{Registry<T extends Identifiable>} \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/model/common/Registry.java#L18}{ad esempio}
    \item Stream, lambda, method reference e Optional per sommare i bonus dei continenti \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/model/map/GameMapImpl.java#L85-L90}{ad esempio}
    \item Ricerca in ampiezza (algoritmo classico di visita dei grafi, descritto per esempio in Cormen et al., \emph{Introduction to Algorithms}) sui territori di un giocatore, per controllare lo spostamento \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/model/map/GameMapImpl.java#L93-L121}{ad esempio}
    \item Record per la trasformazione delle coordinate \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/view/map/ViewTransform.java#L13}{ad esempio}
    \item Lambda per gli eventi di JavaFX sul \texttt{Canvas} \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/view/map/MapCanvas.java#L93-L95}{ad esempio}
    \item JavaFX per la parte grafica: il \texttt{Canvas} su cui disegno mappa e dadi, e il legame tra le sue dimensioni e quelle della finestra \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/view/map/MapCanvas.java#L89-L100}{ad esempio}
    \item Varargs nei test, per passare le righe di un file finto direttamente come argomenti \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/test/java/it/unibo/risiko/model/map/MapLoaderTest.java#L60-L62}{ad esempio}
    \item Switch con le frecce per smistare le righe del file della mappa \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/model/map/MapLoader.java#L82-L99}{ad esempio}
    \item \texttt{Registry} implementa \texttt{Iterable}, così territori e continenti si scorrono con un for-each \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/model/common/Registry.java#L106-L110}{ad esempio}
\end{itemize}
Per alcune parti, segnalate qui sotto, ho preso spunto da guide e discussioni sui forum:
\begin{itemize}
    \item La formula che decide se il numero delle armate va scritto in bianco o in nero sopra il colore del giocatore \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/view/map/MapCanvas.java#L303-L308}{qui}: i pesi 0.299, 0.587 e 0.114 sono quelli della luminosità percepita dello standard ITU-R BT.601.
    \item Il calcolo per adattare la mappa alla finestra senza deformarla e tenerla centrata \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/view/map/ViewTransform.java#L22-L28}{qui}: la scala è il minimo tra i due rapporti di larghezza e altezza, e i margini servono per centrarla.
    \item Il modo per far seguire al \texttt{Canvas} la grandezza della finestra, visto che in JavaFX non si ridimensiona da solo: gli leghi larghezza e altezza a quelle del contenitore \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/view/GameScene.java#L103-L104}{qui} e lo ridisegni quando cambiano \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/main/java/it/unibo/risiko/view/map/MapCanvas.java#L93-L94}{qui}.
    \item Il metodo dei test che trasforma delle righe di testo in un \texttt{InputStream}, così \texttt{MapLoader} e \texttt{MapLayout} leggono un file finto senza doverlo creare davvero \href{https://github.com/MoneIsProgrammer/risiko/blob/90268fcf19c4a90738d5f2d6c9d17ea0389a93c0/src/test/java/it/unibo/risiko/model/map/MapLoaderTest.java#L60-L62}{qui}: questo l'ho copiato così com'è da una discussione su un forum, e lo stesso metodo è anche in \texttt{MapLayoutTest}.
\end{itemize}

%
%
%
Questa sezione, come quella riguardante il design dettagliato va svolta \textbf{singolarmente da ogni membro del gruppo}.
%
Nella prima parte, ciascuno dovrà mostrare degli esempi di codice particolarmente ben realizzati,
che dimostrino proefficienza con funzionalità avanzate del linguaggio e capacità di spingersi oltre le librerie mostrate a lezione.

\begin{itemize}
	\item \textbf{Elencare} (fare un semplice elenco per punti, non un testo!) le feature \textit{avanzate} del linguaggio e dell'ecosistema Java che sono state
utilizzate. Le feature di interesse sono:
	\begin{itemize}
		\item Progettazione con generici, ad esempio costruzione di nuovi tipi generici, e uso di generici bounded.
		L'uso di classi generiche di libreria non è considerato avanzato.
		\item Uso di lambda expressions
		\item Uso di \texttt{Stream}, di \texttt{Optional} o di altri costrutti funzionali
		\item Uso di reflection
		\item Definizione ed uso di nuove annotazioni
		\item Uso del Java Platform Module System
		\item Uso di parti della libreria JDK non spiegate a lezione (networking, compressione, parsing XML, eccetera...)
		\item Uso di librerie di terze parti (incluso JavaFX): Google Guava, Apache Commons...
	\end{itemize}
	\item Si faccia molta attenzione a non scrivere banalità, elencando qui features di tipo ``core'', come le eccezioni, le enumerazioni, o le inner class: nessuna di queste è considerata avanzata.
	\item Per ogni feature avanzata, mostrata, includere:
	\begin{itemize}
		\item Nome della feature
		\item Permalink GitHub al punto nel codice in cui è stata utilizzata
	\end{itemize}
\end{itemize}

In questa sezione, \textit{dopo l'elenco},
vanno menzionati ed attributi con precisione eventuali pezzi di codice ``riadattati'' (o scopiazzati...) da Internet o da altri progetti,
pratica che tolleriamo ma che non raccomandiamo.
%
Si rammenta agli studenti che non è consentito partire da progetti esistenti e procedere per modifiche successive.
%
Si ricorda anche che i docenti hanno in mano strumenti antiplagio piuttosto raffinati e che ``capiscono'' il codice e la storia delle modifiche del progetto,
per cui tecniche banali come cambiare nomi (di classi, metodi, campi, parametri, o variabili locali),
aggiungere o togliere commenti,
oppure riordinare i membri di una classe vengono individuate senza problemi.
%
Le regole del progetto spiegano in dettaglio l'approccio dei docenti verso atti gravi come il plagiarismo.

I pattern di design \textbf{non} vanno messi qui.
%
L'uso di pattern di design (come suggerisce il nome) è un aspetto avanzato di design, non di implementazione,
e non va in questa sezione.

\subsection*{Elementi positivi}

\begin{itemize}
	\item Si elencano gli aspetti avanzati di linguaggio che sono stati impiegati
	\item Si elencano le librerie che sono state utilizzate
	\item Per ciascun elemento, si fornisce un permalink
	\item Ogni permalink fa riferimento ad uno snippet di codice scritto dall'autore della sezione (i docenti verificheranno usando \texttt{git blame})
	\item Se si è utilizzato un particolare algoritmo, se ne cita la fonte originale.
	Ad esempio, se si è usato Mersenne Twister per la generazione di numeri pseudo-random, si cita \cite{mersenne}.
	\item Si identificano parti di codice prese da altri progetti, dal web, o comunque scritte in forma originale da altre persone.
	In tal senso, si ricorda che agli ingegneri non è richiesto di re-inventare la ruota continuamente:
	se si cita debitamente la sorgente è tollerato fare uso di di snippet di codice open source per risolvere velocemente problemi non banali.
	Nel caso in cui si usino snippet di codice di qualità discutibile,
	oltre a menzionarne l'autore originale si invitano gli studenti ad adeguare tali parti di codice agli standard e allo stile del progetto.
	Contestualmente, si fa presente che è largamente meglio fare uso di una libreria che copiarsi pezzi di codice:
	qualora vi sia scelta (e tipicamente c'è), si preferisca la prima via.
\end{itemize}

\subsection*{Elementi negativi}
\begin{itemize}
	\item Si elencano feature core del linguaggio invece di quelle segnalate. Esempi di feature core da non menzionare sono:
    \begin{itemize}
        \item eccezioni;
        \item classi innestate;
        \item enumerazioni;
        \item interfacce.
    \end{itemize}
	\item Si elencano applicazioni di terze parti (peggio se per usarle occorre licenza, e lo studente ne è sprovvisto) che non c'entrano nulla con lo sviluppo, ad esempio:
    \begin{itemize}
        \item Editor di grafica vettoriale come Inkscape o Adobe Illustrator;
        \item Editor di grafica scalare come GIMP o Adobe Photoshop;
        \item Editor di audio come Audacity;
        \item Strumenti di design dell'interfaccia grafica come SceneBuilder: il codice è in ogni caso inteso come sviluppato da voi.
    \end{itemize}
	\item Si descrivono aspetti di scarsa rilevanza, o si scende in dettagli inutili.
	\item Sono presenti parti di codice sviluppate originalmente da altri che non vengono debitamente segnalate.
	In tal senso, si ricorda agli studenti che i docenti hanno accesso a tutti i progetti degli anni passati,
	a Stack Overflow,
	ai principali blog di sviluppatori ed esperti Java,
	ai blog dedicati allo sviluppo di soluzioni e applicazioni
	(inclusi blog dedicati ad Android e allo sviluppo di videogame),
	nonché ai vari GitHub, GitLab, e Bitbucket.
	Conseguentemente, è \emph{molto} conveniente \emph{citare} una fonte ed usarla invece di tentare di spacciare per proprio il lavoro di altri.
	\item Si elencano design pattern
\end{itemize}

\subsection{Esempio}

\subsubsection{Utilizzo della libreria SLF4J}

Utilizzata in vari punti.
Un esempio è \url{https://github.com/AlchemistSimulator/Alchemist/blob/5c17f8b76920c78d955d478864ac1f11508ed9ad/alchemist-swingui/src/main/java/it/unibo/alchemist/boundary/swingui/effect/impl/EffectBuilder.java#L49}

\subsubsection{Utilizzo di \texttt{LoadingCache} dalla libreria Google Guava}

Permalink: \url{https://github.com/AlchemistSimulator/Alchemist/blob/d8a1799027d7d685569e15316a32e6394632ce71/alchemist-incarnation-protelis/src/main/java/it/unibo/alchemist/protelis/AlchemistExecutionContext.java#L141-L143}

\subsubsection{Utilizzo di \texttt{Stream} e lambda expressions}

Usate pervasivamente. Il seguente è un singolo esempio.
Permalink: \url{https://github.com/AlchemistSimulator/Alchemist/blob/d8a1799027d7d685569e15316a32e6394632ce71/alchemist-incarnation-protelis/src/main/java/it/unibo/alchemist/model/ProtelisIncarnation.java#L98-L120}

\subsubsection{Scrittura di metodo generico con parametri contravarianti}

Permalink: \url{https://github.com/AlchemistSimulator/Alchemist/blob/d8a1799027d7d685569e15316a32e6394632ce71/alchemist-incarnation-protelis/src/main/java/it/unibo/alchemist/protelis/AlchemistExecutionContext.java#L141-L143}

\subsubsection{Protezione da corse critiche usando \texttt{Semaphore}}

Permalink: \url{https://github.com/AlchemistSimulator/Alchemist/blob/d8a1799027d7d685569e15316a32e6394632ce71/alchemist-incarnation-protelis/src/main/java/it/unibo/alchemist/model/ProtelisIncarnation.java#L388-L440}


\chapter{Commenti finali}

In quest'ultimo capitolo si tirano le somme del lavoro svolto e si delineano eventuali sviluppi
futuri.

\textit{Nessuna delle informazioni incluse in questo capitolo verrà utilizzata per formulare la valutazione finale}, a meno che non sia assente o manchino delle sezioni obbligatorie.
%
Al fine di evitare pregiudizi involontari, l'intero capitolo verrà letto dai docenti solo dopo aver formulato la valutazione.

\section{Autovalutazione e lavori futuri}
\subsection{Morri Simone}
Ritengo che la mia parte sia in parte troppo ingegnerizzata sia che si mantenga in piedi grazie a pezze e fix, questo forse è dovuto al tempo mal gestito, a una poca comunicazione iniziale e alla mala gestione delle parti in comune. La mia parte la credo forte in quanto più punti sono stati gestiti per avere una quanto più facile integrazione nelle altre componenti. Una evidente debolezza è la troppa dipendenza dalle componenti dei miei compagni che non sono riuscito a risolvere anche a livello teorico.

\subsection{Zangheri Marco}
Mi sono occupato della mappa e dei continenti, dei dadi e del combattimento, del disegno della mappa e della gestione dei click. Siccome la mia parte è quella su cui poggiano le altre, ho cercato di chiudere presto le interfacce, tenerle piccole e non dipendere dalle classi degli altri: la mappa usa solo id e la grafica si usa solo tramite \texttt{MapView}. Secondo me è stata la scelta giusta, perché quando gli altri hanno collegato le loro parti non ho dovuto cambiare niente di importante.

Però ci sono cose che potevo fare meglio. La prima è il disegno: i territori sono cerchi collegati da linee e non le forme vere dei continenti, quindi si capisce chi confina con chi ma non sembra un tabellone. In più \texttt{MapCanvas} è la mia classe più grande e fa un sacco di cose insieme, l'avrei dovuta dividere in due, una per i continenti e una per i territori.

La seconda sono gli id: usare le \texttt{String} ha tenuto separate mappa e giocatori, però il compilatore non distingue l'id di un territorio da quello di un giocatore, e se ne passi uno sbagliato te ne accorgi solo quando il programma gira. Col senno di poi avrei fatto due record piccoli, \texttt{TerritoryId} e \texttt{PlayerId}.

La terza è che la mappa si fida troppo di chi la usa: \texttt{GameMap} restituisce i territori veri, quindi chiunque può cambiare proprietario e armate senza passare da nessuna regola, e lo stesso vale per il combattimento, visto che \texttt{resolve} calcola e basta. Erano scelte per tenere le cose semplici e testabili, ma potevo dare i territori in sola lettura e mettere nella mappa dei metodi che fanno i controlli.

Queste cose le ho viste verso la fine, soprattutto per il tempo e per la poca comunicazione che c'è stata tra di noi all'inizio: alcune decisioni che riguardavano tutti e tre le abbiamo prese ognuno per conto suo.

Nella proposta iniziale mi ero preso anche il salvataggio della partita, che era fra le funzionalità opzionali, e alla fine non sono riuscito a farlo. Qui il problema è stato come ho gestito il tempo: ho scelto di dare la precedenza alle parti obbligatorie e ai test, e quando le ho chiuse mi sono accorto che non ne restava abbastanza per fare bene anche questa. Salvare una partita infatti non vuol dire salvare solo la mappa, ma tutto lo stato del gioco, e farlo di corsa avrebbe voluto dire consegnare una funzione a metà. Ho preferito consegnare meno cose, ma finite e testate. La strada comunque è aperta: la mappa si carica già da un file di testo, quindi per salvarla basterebbe riscrivere lo stesso formato con proprietari e armate aggiornati, e nel menu il pulsante per ricaricare una partita è già predisposto, solo disattivato.

Se si andasse avanti, la prima cosa che farei è una mappa a poligoni: le coordinate stanno già in un file separato dal model, quindi basterebbe estendere \texttt{MapLayout} per fargli leggere una lista di punti invece di un punto solo.


\textbf{È richiesta una sezione per ciascun membro del gruppo, obbligatoriamente}.
%
Ciascuno dovrà autovalutare il proprio lavoro, elencando i punti di forza e di debolezza in quanto prodotto.
Si dovrà anche cercare di descrivere \emph{in modo quanto più obiettivo possibile} il proprio ruolo all'interno del gruppo.
Si ricorda, a tal proposito, che ciascuno studente è responsabile solo della propria sezione: non è un problema se ci sono opinioni contrastanti, a patto che rispecchino effettivamente l'opinione di chi le scrive.
Nel caso in cui si pensasse di portare avanti il progetto, ad esempio perché effettivamente impiegato, o perché sufficientemente ben riuscito da poter esser usato come dimostrazione di esser capaci progettisti, si descriva brevemente verso che direzione portarlo.

\section{Difficoltà incontrate e commenti per i docenti}

\subsection{Zangheri Marco}
Scrivere il codice ha avuto le sue difficoltà, ma la più grossa è stata lavorare in tre su parti che si toccano. Ognuno portava avanti la sua, e le cose in comune, come gli eventi o il modo di identificare territori e giocatori, a volte cambiavano mentre qualcuno le stava già usando. Ho capito che quelle vanno decise tutti insieme all'inizio, prima di cominciare.

Dal lato opposto, le cose che mi porto dietro da questo progetto sono soprattutto due. La prima è Git: non l'avevo mai usato prima, e lavorando in tre ho imparato a usarlo sul serio, con i commit, i branch e le pull request su un repository condiviso. La seconda è quanto conta separare bene le parti di un programma. Tenendo la mappa e il combattimento senza niente di grafico, ho potuto testarli da soli, senza aprire nessuna finestra, e il controller non sa nemmeno che la mappa è disegnata con JavaFX. Quando ho dovuto cambiare il disegno, per esempio per la linea fra Alaska e Kamchatka, ho toccato solo la parte grafica e il resto è rimasto com'era.


Questa sezione, \textbf{opzionale}, può essere utilizzata per segnalare ai docenti eventuali problemi o difficoltà incontrate nel corso o nello svolgimento del progetto, può essere vista come una seconda possibilità di valutare il corso (dopo quella offerta dalle rilevazioni della didattica) avendo anche conoscenza delle modalità e delle difficoltà collegate all'esame, cosa impossibile da fare usando le valutazioni in aula per ovvie ragioni.
%
È possibile che alcuni dei commenti forniti vengano utilizzati per migliorare il corso in futuro: sebbene non andrà a vostro beneficio, potreste fare un favore ai vostri futuri colleghi.
%
Ovviamente \textit{il contenuto della sezione non impatterà il voto finale}.

\appendix
\chapter{Guida utente}

Capitolo in cui si spiega come utilizzare il software. Nel caso in cui il suo uso sia del tutto
banale, tale capitolo può essere omesso.
%
A tal riguardo, si fa presente agli studenti che i docenti non hanno mai utilizzato il software
prima, per cui aspetti che sembrano del tutto banali a chi ha sviluppato l'applicazione possono non
esserlo per chi la usa per la prima volta.
%
Se, ad esempio, per cominciare una partita con un videogioco è necessario premere la barra
spaziatrice, o il tasto ``P'', è necessario che gli studenti lo segnalino.

\subsection*{Elementi positivi}

\begin{itemize}
 \item Si istruisce in modo semplice l'utente sull'uso dell'applicazione, eventualmente facendo uso di schermate e descrizioni.
\end{itemize}

\subsection*{Elementi negativi}
\begin{itemize}
 \item Si descrivono in modo eccessivamente minuzioso tutte le caratteristiche, anche minori, del software in oggetto.
 \item Manca una descrizione che consenta ad un utente qualunque di utilizzare almeno le funzionalità primarie dell'applicativo.
\end{itemize}

\chapter{Esercitazioni di laboratorio}

In questo capitolo ciascuno studente elenca gli esercizi di laboratorio che ha svolto
(se ne ha svolti),
elencando i permalink dei post sul forum dove è avvenuta la consegna.
%
Questa sezione potrebbe essere processata da strumenti automatici,
per cui link a oggetti diversi dal permalink della consegna,
errori nell'email o nel nome del laboratorio possono portare ad ignorare alcune consegne,
si raccomanda la massima precisione.

\section*{Esempio}

\subsection{paolino.paperino@studio.unibo.it}

\begin{itemize}
 \item Laboratorio 04: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=12345#p123456}
 \item Laboratorio 06: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=22222#p222222}
 \item Laboratorio 09: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=99999#p999999}
\end{itemize}

\subsection{paperon.depaperoni@studio.unibo.it}

\begin{itemize}
 \item Laboratorio 04: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=12345#p123456}
 \item Laboratorio 05: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=22222#p222222}
 \item Laboratorio 06: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=99999#p999999}
 \item Laboratorio 07: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=22222#p222222}
 \item Laboratorio 08: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=99999#p999999}
 \item Laboratorio 09: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=22222#p222222}
 \item Laboratorio 10: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=99999#p999999}
 \item Laboratorio 11: \url{https://virtuale.unibo.it/mod/forum/discuss.php?d=22222#p222222}
\end{itemize}


\bibliographystyle{alpha}
\bibliography{13-template}

\end{document}
